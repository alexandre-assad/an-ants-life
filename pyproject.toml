[tool.poetry]
name = "an-ants-life"
version = "0.1.0"
description = ""
authors = ["bthreeIA"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
